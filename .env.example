# Required - one or more "caller:secret" pairs, comma-separated.
# A request is authorized if its token matches any secret.
# Generate a secret with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
ATLAS_TOKEN=claude:changeme

# Optional - defaults shown
PORT=7784
ATLAS_DB_PATH=/app/data/atlas.db
